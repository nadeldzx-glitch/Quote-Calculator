# 2026-08-03 工作日志

## 软件管理 / 安全讨论（未改动源码）

用户连续问了管理类问题：源码位置与链接关系、断网/坏电脑恢复、仅管理员改源码、换电脑换智能体上手；以及"有链接能否用工具拿到源码"。

关键事实（已用工具核实）：
- 源码真相源：本地 `hollow-board-deploy/index.html`（单文件，~2441 行，未混淆）
- 链接 = CloudStudio 沙箱副本，单向解耦；源电脑离线不影响链接运行（仅 OCR 的 CDN 依赖）
- 运行时唯一外部依赖：Tesseract.js CDN（仅 OCR）；无后端、无数据库、数据在 localStorage
- app 内 admin/employee 密码（`dwb135790`/`dongjie345`）只管界面按钮（.admin-only），不管源码编辑

### 实操验证"有链接即可下载源码"（Craft 模式演示）
- 用 `curl` 从 `https://f2dace729d844584ae23ed7275c41088.sh5.agentos-app.net/` 下载成功：
  - `index.html` 145442B（含 checkAuth/calcStep1/TEMPLATE_VERSION=164/dwb135790 明文）
  - `DEV_GUIDE.md` 10175B（开发文档也一并暴露）
  - `_archive/_full_v118_backup.html` 145442B（旧版源码备份也暴露）
- 下载件保存在 `G:\WorkBuddy 存储\2026-07-29-23-12-13\downloaded_source\`
- 结论：任何人拿到链接即可用浏览器/curl 取得完整明文源码，且部署目录里文档与备份也公开。
- 未暴露：本地路径、未部署的改动、`.workbuddy/memory/`、他人浏览器 localStorage 模板。

### 待用户决策的下步防护
1. 把 `DEV_GUIDE.md`、`_archive/` 移出部署目录，重新部署（只上传 index.html）
2. 给 CloudStudio 链接加访问限制（密码/白名单，平台侧设置，待查官方文档）
3. 可选：JS 混淆压缩（仅提高抄袭成本，非绝对保密）
4. 跨电脑上手：把源码+`.workbuddy/memory/`+根目录 README 进 Git 仓库，新机器 clone 即上手
