# 2026-07-30

## 中空板算价器 HTML 修改(按用户要求)

### 当日要点
- 基础版构建与多轮迭代**一天完成**,从空白模板演进到 v101(当前部署版)
- 主导思路:用户通常发**截图+一句简短说明**(图片中的红/绿框是关键沟通媒介)
- 大量迭代集中在打印单"中栏计算过程"的格式上——逐步逼近 calcStep1~4(价格计算模块)的 cpLine 四段式风格

### 今日修改摘要(从 v89 到 v101,共 13 版)
完整实施步骤已精简,本节只留**最终结论和目的**。如需追溯版本细节,git log 或新会话中 grep 版本号即可。

| 版本 | 主要改动 |
|---|---|
| v89–v91 | 打印单去掉冗余条目(平方价/板材单价等)+ 加板材售价(单);焊底式加体积调整系数 |
| v92 | 焊底式打印单 ① 展示两套板材(板材1=纸箱式 / 板材2=1.5×F+G+Loff),单产品面积=(Im×Jm + Ia×Ja)×使用率 |
| v93 | 平方价折叠进板材售价(单) = 面积×克重×原料售价÷1e6 |
| v94 | 删除重复"是否含税"sub-label;在"原料配方"上方加"原料/板材价格计算"二级标题;**打印报价单改 A4 横版**(1123×794,@page landscape) |
| v95 | 删 calcStep1 的"自动把 dimType 设为外尺"逻辑——箱子没选外/内尺时整段不渲染,加灰色提示 |
| v96 | 删打印单 ③ 里的"成品价格(单)"(v96 → v98 之间反复修正);`_printOption` 默认改 `'verify'`,打开弹窗时也设 verify |
| v97 | ③ 后加工 → ③ 后加工价格计算;④ 附加费用 → ④ 附加费用计算;红框按绿框格式补全公式 |
| v98 | **圆盘打印单 ① 总立方数公式**用 E²×A×H÷1e9(与 calcStep1 一致);其他样式补 ÷1000;总吨数补 ÷1e6 |
| v99 | 打印单 ② 原料成本/原料售价去掉"公式文字"部分,只保留 label+代入值+结果 |
| v100 | 打印单"后加工费用合计"代入值带括号注释:`0.50(普通钉)+1.00(铆钉)+...`,与 calcStep3 完全对齐 |
| v101 | 打印单 ③ 加回"成品价格(单)"行(v96 删的)+ ④ 改回 calcLine 四段式(全角括号、含税/不含税分支公式) |

### 沟通约定(已写进 MEMORY.md 顶部)
- 用户截图里**绿框**通常是"参考格式"或"保留内容",**红框**通常是"要去掉"
- 若多个同色框,必须读图片文字(JS 读取失败时靠描述+grep 定位)
- "格式参考 XX" → 意思是和 calcStepN 的实现保持对齐,通常要两侧同步改

### 已知小问题(暂未修)
- 用户在 v101 后没再发反馈,以上 v101 是当前线上版
- sed 在中文路径上会出错,统一改用 awk 提取 script 块做 node --check 校验

### 部署信息(2026-07-31 10:08 末次更新)
- 部署地址:https://f2dace729d844584ae23ed7275c41088.sh5.agentos-app.net
- 沙箱 ID:f2dace729d844584ae23ed7275c41088
- 当前线上版:v101
- 本地路径:G:\WorkBuddy 存储\2026-07-29-23-12-13\hollow-board-deploy\index.html
- 项目状态(详细):同目录 MEMORY.md
